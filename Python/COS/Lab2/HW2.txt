2. Реализовать вычисление кратковременной энергии для сигнала. Исходный сигнал разбивается на интервалы длины N. Интервалы перекрываются наполовину (последующий начинается в середине предыдущего). Для каждого интервала расчитывается сумма квадратов амплитуды. Полученные данные отображаются в виде графика.

3. Реализовать вычисление zero-crossing для сигнала. Исходный сигнал разбивается на интервалы длины N. Интервалы перекрываются наполовину (последующий начинается в середине предыдущего). Для каждого интервала расчитывается число пересечений сигнала нуля. Полученные данные отображаются в виде графика.

4. Реализовать вычисления отношения сигнал-шум. Для проверки сгенирировать:
 - синусоидальный сигнал с частотой 10Гц. Взять 1000 отсчетов. 
 - случайный сигнал с равномернм распределением. Взять 1000 отсчетов.
 Вывести теоретическое и смоделированное значения snr.